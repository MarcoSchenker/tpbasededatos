<!DOCTYPE html>
<html lang="es">
<head>
  <title>MovieWeb</title>
  <style>
    /* Estilos generales */
    body {
      font-family: 'Arial', sans-serif;
      background-color: #f0f0f0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      padding: 20px;
    }

    h1 {
      font-size: 36px;
      color: #333;
      margin-bottom: 30px;
      text-align: center;
    }

    form {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 10px; /* Espacio entre los elementos */
      background-color: white;
      padding: 20px;
      border-radius: 40px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Sombra para dar relieve */
    }

    /* Estilo de los inputs */
    input[type="text"], select {
      padding: 12px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 40px;
      width: 250px;
      outline: none;
      transition: border-color 0.3s ease;
    }

    input[type="text"]:focus, select:focus {
      border-color: #0078d4;
    }

    /* Estilo del botón */
    button[type="submit"] {
      background-color: #0078d4;
      color: white;
      border: none;
      border-radius: 40px;
      padding: 12px 20px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button[type="submit"]:hover {
      background-color: #0056b3;
    }

    /* Estilo del selector */
    select {
      padding: 12px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 40px;
      width: 100px; /* Tamaño reducido */
      outline: none;
      transition: border-color 0.3s ease;
      background-color: #f5f5f5;
      background-image: url('data:image/svg+xml;charset=UTF-8,%3Csvg xmlns%3D%22http%3A//www.w3.org/2000/svg%22 viewBox%3D%220 0 24 24%22 fill%3D%22%23333%22%3E%3Cpath d%3D%22M7 10l5 5 5-5z%22/%3E%3C/svg%3E'); /* Flecha personalizada */
      background-repeat: no-repeat;
      background-position: right 10px center; /* Mueve la flecha 15px desde la derecha */
      background-size: 16px;
      appearance: none; /* Oculta la flecha predeterminada del sistema */
      -webkit-appearance: none;
      -moz-appearance: none;
    }

    select:hover, input[type="text"]:hover {
      border-color: #0078d4;
    }
  </style>
</head>
<body>
<h1>MovieWeb</h1>
<!-- Así se comenta, god-->
<!-- En la main page ahora hay una caja en la cual podemos seleccionar que tipo buscar (película, actor o director) -->
<form action="/buscar" method="GET">
  <select name="type" required>
    <option value="movie">Película</option>
    <option value="actor">Actor</option>
    <option value="director">Director</option>
    <option value="todo">Search All</option> <!--agrego selector de buscar por all-->
    <option value="keyword">Keyword</option><!--agrego selector de buscar por keyword -->
  </select>
  <input type="text" name="q" placeholder="Buscar..." required />
  <button type="submit">Buscar</button>
</form>


<!-- Para el recuerdo:

<form action="/buscarActor" method="GET"> 
  <input type="text" name="q" placeholder="Buscar actor..." />
  <button type="submit">Buscar</button>
</form>
<form action="/buscarCrew" method="GET"> 
  <input type="text" name="q" placeholder="Buscar crew..." />
  <button type="submit">Buscar</button>
</form>
-->
</body>
</html> 